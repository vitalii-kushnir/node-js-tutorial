extends layout

block content
    .inner
        h2= title
        .map
            .autocomplete
                input.autocomplete__input(type="text" plcaholder="Search for Anything" name="geolocate")
            #map
                p Loading Map...